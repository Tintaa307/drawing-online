import React from "react"

const Loader = () => {
  return (
    <main className="relative w-full h-screen flex items-center justify-center bg-bg_color z-50">
      <div className="loader"></div>
    </main>
  )
}

export default Loader
